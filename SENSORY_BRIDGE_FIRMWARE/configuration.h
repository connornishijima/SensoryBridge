//-------------------------------
  // Used to allow for custom LED strip lengths!
  // Be aware that no matter what you set here,
  // 128 LEDs is still the internal resolution used,
  // which will be interpolated (with CPU overhead)
  // to match the length you choose!
  #define STRIP_LED_COUNT  128
//-------------------------------
